apiVersion: v1
kind: Service
metadata:
  name: devops-challenge-svc
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "elb"
spec:
  type: LoadBalancer
  selector:
    app: devops-challenge
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
